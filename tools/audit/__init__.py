"""Audit scripts for the cuSFM paper claims (`docs/paper-claims-audit.md`).

Nothing here is part of the `colsfm` package: these are one-off measurement
harnesses that import `colsfm` read-only and write their outputs under
`data/bench/audit_*` and `data/cusfm_runs/audit_*`.
"""
